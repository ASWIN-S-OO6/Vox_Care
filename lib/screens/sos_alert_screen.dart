import 'package:flutter/material.dart';

class SosAlertScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SOS Alert Calls'),
      ),
      body: Center(
        child: Text('Manage SOS alert calls.'),
      ),
    );
  }
}
