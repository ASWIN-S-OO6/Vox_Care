import 'package:flutter/material.dart';

class CaretakerNotifyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Notify Caretaker'),
      ),
      body: Center(
        child: Text('Notify your caretaker here.'),
      ),
    );
  }
}
